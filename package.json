{
  "name": "@xgsd/engine",
  "description": "lightweight execution/orchestration library used by xGSD apps",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn clean && tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^29.x",
    "ts-jest": "^29.x",
    "ts-node": "^10.9.2",
    "typescript": "^5.x"
  }
}
